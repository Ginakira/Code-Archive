# 回文数

```c++
int ans (int num) {
    int t;
    int sum = 0;
    t = num;
    while (num) {
        sum = sum * 10 + num % 10;
        num /= 10;
    }
    if (t == sum) {
        return true;
    } else {
        return "假";
    }
}
```

