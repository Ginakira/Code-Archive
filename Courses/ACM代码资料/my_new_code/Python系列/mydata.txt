Turn uppercase letters into lowercase letters ：str.lower()
Turn lowercase letters into uppercase letters ：str.upper()
输出大小要在print里使用 例如: print(str.upper())
Print multiple starts : print(number * '*')
Return the first letter subscript ： str.find(str1) 找到第一个
Returns the number of occurrences of strings : str.count(str1)
Returns the specified character connection : str2 = str.join(str1)
The return value is the corresponding decimal integer : n = ord(c)
The return value is the ASCII character corresponding to the current integer : c = chr(c)
Add a new list content to an existing list : list.extend(list1) 一次多个
Add new objects at the end of the list : list.append(c) 一次一个
from sys import stdout    stdout.write()
import random 先导入库
random.random() 用于生成一个0到1的随机符点数
random.uniform(a, b) 用于生成一个指定范围内的随机符点数 下限可以小于上限
random.randint(a, b) 用于生成一个指定范围内的整数
add = lambda x, y : x+y 匿名函数
import calendar print(calendar.month(yy,mm))
list.insert(index, obj) 将指定对象插入列表的指定位置
list.remove(obj) 移除列表中某个值的第一个匹配项
end可以用于将结果输出到同一行，或者在输出的末尾添加不同的字符 
print(b, end=',')
string   
getline(cin,a)  输入一整行
b[i] = tolower(b[i])  小写     toupper  大写
b.find(a)   找是否包含a字符串返回首次索引   与string::npos 比较  它相当于-1
例如   if(b.find(a) == string::npos) {cout<<-1<<endl;}
The max number: (1<<31)-1
You can write this when you want to write some numbers in a line: a,b = map(int,input().split())
delete a list (the lost ont element): a = []   a.pop()   or a.pop(-1) 
arr = [0 for i in range(l+1)]
#include <ctype.h>  isalpha( int ch );如果参数是字母字符，函数返回非零值，否则返回零值。
　　