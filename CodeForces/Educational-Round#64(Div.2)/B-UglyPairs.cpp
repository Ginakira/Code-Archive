#include <iostream>
#include <string>
#include <queue>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    int t;
    cin >> t;
    while(t--) {
        string s;
        queue<char> out;
        cin >> s;
        for(int i = 1; i < s.size(); ++i) {
            if(s[i] == s[i - 1] + 1 || s[i] == s[i - 1] - 1) void;
        }
    }

    return 0;
}