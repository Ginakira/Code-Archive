#include <iostream>
#include <algorithm>
#include <set>
using namespace std;

int p[300010][2];
int main() {
    int n, m;
    cin >> n >> m;
    for(int i = 0; i < m; ++i) {
        cin >> p[i][0] >> p[i][1];
    }
    
    return 0;
}